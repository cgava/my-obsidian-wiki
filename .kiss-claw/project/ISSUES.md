


# Open Questions

## Infra

Déclencher l'ajout via git (le raw est dirty), en plus ça permettrait de rajouter les hooks directement associés à git, et ça gère le SHA256 en natif


## Business

-> si on modifie un projet (on l'a renommé dans les prompts par exemple) : comment est-on censé maintenir le dépot consistant ?
-> j'ai ajouté un nouveau concept qui me semble important, directement depuis la partie "wiki"... comment lui mettre à jour le frontmatter 

-> j'ai un nouveau type d'input = c'est du prompt, prétraité
-> j'ai un nouveau type d'input = c'est du prompt, prétraité, qui a généré un document (comment ajouter une type d'input, qui doit être traité de façon particulière) ?

-> j'ai une catégorie particulière = un projet entier depuis git
** comment filtrer les dossiers // fichiers à ne pas indexer ?
** faut-il cloner tout le dépot ? comment gere-t-on les mises à jour ? peut-on "archiver" un depot (et dans ce cas, probablement ne plus le suivre) ?